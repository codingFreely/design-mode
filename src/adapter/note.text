适配器模式：
旧接口格式和新使用者所需格式不兼容，新接口和旧的使用者不兼容
中间加一个适配器进行转换

现实场景：插座转换器

使用场景
1.封装旧接口
例：老代码中使用$.ajax发送请求，新代码中弃用了jquery，并自己封装了一个ajax方法，那老代码中$.ajax如何处理？
解：
自己封装一层适配器
const $ = {
    ajax(options) {
        return newAjax(options)
    }
}

设计原则验证：
将旧接口和使用者进行分离，符合开放封闭原则
